name: Apply BIZNEXCO Visual Theme

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  visual-theme:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Repair report HTML script string
        shell: python
        run: |
          from pathlib import Path
          p = Path('index.html')
          text = p.read_text(encoding='utf-8')
          old1 = '+"<script src=\'visual-theme.js?v=1\'><\\/script>\n<script src="visual-theme.js?v=1"></script>\n</body></html>";'
          new1 = '+"<script src=\'visual-theme.js?v=1\'><\\/script>\n</body></html>";'
          old2 = '+"<script src="visual-theme.js?v=1"></script>\n</body></html>";'
          new2 = '+"<script src=\'visual-theme.js?v=1\'><\\/script>\n</body></html>";'
          changed = False
          if old1 in text:
              text = text.replace(old1, new1, 1)
              changed = True
              print('Removed duplicate broken script tag from report string')
          if old2 in text:
              text = text.replace(old2, new2, 1)
              changed = True
              print('Repaired broken report script string')
          if changed:
              p.write_text(text, encoding='utf-8')
          else:
              print('Report script string already repaired')

      - name: Add visual theme to index.html
        shell: python
        run: |
          from pathlib import Path
          p = Path('index.html')
          text = p.read_text(encoding='utf-8')
          marker = '<script src="visual-theme.js?v=1"></script>'
          if marker not in text:
              if '</body>' in text:
                  text = text.replace('</body>', marker + '\n</body>', 1)
              else:
                  text += '\n' + marker + '\n'
              p.write_text(text, encoding='utf-8')
              print('Visual theme script added to index.html')
          else:
              print('Visual theme script already present')

      - name: Commit HTML update
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add index.html
          if git diff --cached --quiet; then
            echo "No HTML changes required"
          else
            git commit -m "Repair broken report HTML script string"
            git push
          fi
